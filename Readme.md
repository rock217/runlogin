
# runlogin

server side mode!

1. clone this repo and cd to its directory
2. run `node server.js`
3. visit `http://localhost:3000` in your browser
4. use `admin@example.com/hellothere` as `email/password`